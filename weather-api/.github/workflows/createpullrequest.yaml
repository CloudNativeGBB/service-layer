# name: Create Pull Request
# on: push
# jobs:
#   createPullRequest:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - name: Update app file
#         run: date +%s > report.txt
#       - name: Create Pull Request
#         id: arc
#         uses: peter-evans/create-pull-request@v2
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           commit-message: Update app manifest file
#           committer: Cloud Native GBB <cloudnativegbb@users.noreply.github.com>
#           title: 'Updated Weather API'
#           body: |
#             New report
#             - Contains *today's* date
#             - Auto-generated by [create-pull-request][1]

#             [1]: https://github.com/peter-evans/create-pull-request
#           branch: app-baseline
#       - name: Check outputs
#         run: |
#           echo "Pull Request Number - ${{ env.PULL_REQUEST_NUMBER }}"
#           echo "Pull Request Number - ${{ steps.arc.outputs.pr_number }}"
